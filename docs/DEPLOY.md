# obs-deploy — Deploy (this tool)

This repo is the deploy tool itself; it is not copied into Obsidian vaults.

Release to your machine: `./scripts/install.sh` from the repo root (copies into `~/.local/bin`, not `npm link`).

User machine config: `~/.config/obsidian/deploy.json` (see `config/deploy.json.example`).
