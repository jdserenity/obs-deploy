{
  "name": "obs-deploy",
  "version": "0.1.0",
  "description": "Build and copy Obsidian plugin artifacts into configured vaults",
  "type": "module",
  "bin": {
    "obs-deploy": "./bin/obs-deploy.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "install-local": "bash scripts/install.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
